.pio
.vscode/
.claude/
.DS_Store

# blog-admin: local-only post-authoring tool, never deployed to Pages.
# Its generated output (blog/, blog.html, assets/images/blog/) is NOT
# covered by this rule and is committed normally.
blog-admin/

# Local quick-launch shortcut for blog-admin (hardcoded machine-local
# paths, macOS-only — never part of the deployed site).
CV Blog Post.app/

# impeccable design-hook local dedup cache — not meant to be shared/versioned;
# .impeccable/config.json (the actual suppression rules) IS committed normally.
.impeccable/hook.cache.json

# PlatformIO firmware project (unrelated to the portfolio site in this repo)
src/
include/
lib/
test/
platformio.ini
